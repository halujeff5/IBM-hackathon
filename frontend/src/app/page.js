import TelemetryStream from "./TelemetryStream";
import Landing from "./LandingComponent";

export default function Home() {
  return (
    <main style={{ minHeight: "100vh", background: "#94a3b8" }}>
      <Landing />
      {/* <TelemetryStream /> */}
    </main>
  );
}
